{
  "devDependencies": {
    "@types/node": "^14.11.2",
    "cypress": "^8.2.0",
    "gts": "^3.1.0",
    "typescript": "^4.4.4"
  },
  "scripts": {
    "cy:open": "cypress open",
    "cy:run": "cypress run",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
